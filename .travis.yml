dist: trusty
language: node_js
node_js:
  - "6"
cache:
  directories:
    - node_modules
script: "npm run cover"
after_success:
  - coveralls
deploy:
  provider: npm
  email: ${NPM_EMAIL}
  api_key: ${NPM_API_KEY}
  on:
    tags: true
